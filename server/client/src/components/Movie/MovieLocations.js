import React, { Component } from 'react';
import { connect } from 'react-redux';
class MovieLocations extends Component {
  render() {
    return <div />;
  }
}

function mapStateToProps({ movie, locations }) {
  return movie;
}
export default MovieLocation;

exports.generate_d3_tree_data = (result) => {
  console.log("result in generate: " + JSON.stringify(result));
  let pagesVisited = result.pagesVisited;
  let tree_obj = {
    
  }
  console.log("page visited: " + JSON.stringify(pagesVisited));
}
